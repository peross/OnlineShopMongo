const imagePicker=document.querySelector(".image-upload-preview input"),imagePreview=document.querySelector(".image-upload-preview img");function updateImagePreview(){const e=imagePicker.files;if(!e||0===e.length)return void(imagePreview.style.display="none");const i=e[0];imagePreview.src=URL.createObjectURL(i),imagePreview.style.display="block"}imagePicker.addEventListener("change",updateImagePreview);