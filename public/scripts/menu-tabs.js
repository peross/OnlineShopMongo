const menuTabs=document.querySelector(".menu-tabs"),menuTabSection=document.querySelector(".menu-tab-content");function createProductsList(e){const t=document.createDocumentFragment();for(const n of e){const e=document.createElement("div");e.className="menu-item",e.innerHTML=`\n        \n        <div class="menu-item-title">\n            <h3>${n.name}</h3>\n            \n        </div>       \n        <div class="menu-item-price">\n            <p>${n.price} KM</p>\n        </div>`,t.appendChild(e)}return t}async function fetchCategory(e){if(e.target.classList.contains("menu-tab-item")&&!e.target.classList.contains("active")){const t=e.target.getAttribute("data-category");menuTabs.querySelector(".active").classList.remove("active"),e.target.classList.add("active");const n=await fetch(`/products/${t}`),c=createProductsList(await n.json());menuTabSection.innerHTML="",menuTabSection.appendChild(c)}}window.onload=()=>{document.getElementById("defaultCategory").click()},menuTabs.addEventListener("click",fetchCategory);